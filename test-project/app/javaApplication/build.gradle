apply plugin: 'application'
apply from: "$rootDir/gradle/quality.gradle"

sourceCompatibility = 1.6
version = '1.0'

mainClassName = "test.metric.drakklord.teststudio.apptest"

dependencies {
    compile project(':lib:testJava')
}
